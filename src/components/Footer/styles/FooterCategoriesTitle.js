import styled from 'styled-components';

export const FooterCategoriesTitle = styled.p`
  color: rgb(201, 206, 214);
  font-size: ${({theme}) => theme.sizes.s4};
  padding-bottom : ${({theme}) => theme.sizes.s4};
  padding-top : ${({theme}) => theme.sizes.s4};
`